name: Embodied HA Extensions (development)
version: "0.1.0-dev.3"
slug: embodied_ha_extensions
description: "Development build for curated, opt-in first-party Embodied HA extensions."
stage: experimental
arch:
  - amd64
  - aarch64
init: true
startup: application
boot: manual
hassio_api: true
services:
  - mqtt:need
map:
  - type: homeassistant_config
    read_only: false
    path: /config
options:
  log_level: info
  enabled_extensions: []
  ambient_speech_context:
    retention_hours: 24
    max_lines: 3
schema:
  log_level: list(debug|info|warning|error)
  enabled_extensions:
    - list(ambient_speech_context)
  ambient_speech_context:
    retention_hours: int(1,168)
    max_lines: int(1,20)
