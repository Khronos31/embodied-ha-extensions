# Bundled applications

`ambient_speech_context` is the first fixed, bundled application. It consumes the RTSP Assist Gateway v1 transcript MQTT contract and owns only its bounded files under `/config/embodied-ha-extensions/apps/ambient_speech_context`.

Applications are not discovered from `/config`, and users cannot add entrypoints or packages at runtime.
